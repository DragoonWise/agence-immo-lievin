{% extends 'home/base.html.twig' %}

{% block content %}
{{ form_start(form) }}
{{ form_rest(form) }}
<button type="submit" class="btn btn-primary">Envoyer le message</button>
{{ form_end(form) }}
{% endblock %}